version: 2
updates:
  # Keeps the SHA-pinned actions current; the only dependency ecosystem here.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    cooldown:
      default-days: 7
    groups:
      actions:
        patterns: ["*"]
