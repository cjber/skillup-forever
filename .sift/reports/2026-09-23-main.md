# sift audit — SkillUp Forever @ 92db2b2

Scope: whole repo at origin/main after #27 (generated `Data/*.lua` not reviewed), plus a blind Codex
pass over the same scope. Gate before: pass. Gate after: pass. Suppressions: 0.
Open PR #26 (Shortest Path Forever waypoints) rewrites `Sources.lua`, `docs/*.md` and parts of
`Route.lua`/`Shopping.lua`; findings there are proposals for #26, not edits here.

## Carried forward (2026-09-23.md)

Every "Bugs found, not fixed" item was fixed in #20. Unproven candidates stay open, unchanged:
generator helper duplication, the cloned argparse preamble, `ns.Describe` restating `ns.NetCost`,
price-source strings, `craftValue` options table, the `C_TooltipInfo` guard, own-file `ns.*`
helpers (now also `NearestNPC`, `RouteTarget`, `ShowRecipe`), oversized `Route.lua`, long README
bullets, `model_spec.lua:94` sample checks.

## Changes

| Change | Category | Lines |
|---|---|---|
| `tools/screenshots.py`: `LEATHERWORKING` and `net_cost()` unused (vulture, rg) | dead-code | −9 |
| `tools/gen_thresholds.py`: third tuple element `sorted(seen)` never read; Thresholds.lua regenerates identical | dead-code | ±0 |
| `tools/gen_trainer.py`: `cap > 75` → `RANK_SKILL` | named constant | ±0 |
| `Prices.lua` `ns.CraftValue`: `each` field has no reader (field, comment, `SkillUpValue` type) | dead-code | −1 |
| `Shopping.lua:119`: "planned afresh each time … bags" — plans are cached in `Route.lua` and bags are compared separately | stale comment | ±0 |
| `README.md`: reagent tooltips default to tracked-route needs (Shift or the setting for the recipes that use it); the scan covers reagents and outputs of every opened profession | stale-docs | ±0 |
| `PLAN.md`: marked as the original design plan (it predates the flattened sort, the 4-value thresholds and LuaJIT specs) | stale-docs | +2 |

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `Route.lua:144` | rank fees are picked by `target`, rendering by segment end: a route blocked at 65 of 150 still charges the Journeyman fee it never shows (Codex repro) | pick ranks from the reached skill |
| `Route.lua:469` | unpriced grey recipes count as "unpriced", so the page says "open the auction house" instead of listing `RecipeSuggestions` | count only recipes that still skill up |
| `Prices.lua:229` | an empty live schematic with no bundled data costs 0 (free), though the comment treats empty as unavailable | return nil when neither is present |
| `tests/core_spec.lua:49` | both init callbacks are the same `Init`, so swapping the registrations still passes | give each a distinct stub |
| `tools/screenshots.py:116,144` | ports drift from Model.lua: `model_recipe_cost([])` is None (Lua 0), `round_money(0)` is 1 (Lua 0 since #20) | port the #20 fixes |
| `tools/screenshots.py:160` | comment says Leatherworker/Miner and prices Light Leather as bought; `PROFESSIONS` renders Skinning | pick one scene (changes the PNGs) |

## Proposals (user-facing, or in PR #26's files)

| Location | Proposal |
|---|---|
| `Sources.lua:95` | `NearestNPC(npcIDs, vendorsOnly)`: every caller passes true; fold the dead branch into #26's rewrite |
| `Route.lua:30,31,144,409` | the rank step `75` as a literal; name it when #26 lands |
| `docs/route.md:7` | "visited trainers' fees win" covers recipe fees only; rank fees always come from `ns.TrainerRanks` |
| `Model.lua:233` | Shopping's owned-count callback always returns 0 in production; only tests use it |
| `Prices.lua:49`, `Tooltip.lua:129` | `DAY = 86400` defined twice (minor) |

## False positives

- `README.md:23` trainer `?`: an unmatched service is usually rank training, not a recipe; `?` marks shared names, as the README says.
- Shopping `CALLER` equals `ns.TITLE` by coincidence; it's Auctionator's caller id.

## Learning captured

- Profile: the ty baseline is 6 (screenshots.py adds `wowmock` and the divider tuple); `tools/screenshots.py` is a Python port of Model/Core, so audits check the port against the Lua.
- Rules and lenses: none added.

## Next

Fix the two `Route.lua` bugs after #26 merges, with `route_spec` checks, and port the #20 money fixes to `screenshots.py` together with the WFA-9 screenshot refresh.
