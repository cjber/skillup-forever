# sift audit — SkillUp Forever @ b4e1e9b

Scope: whole repo (generated `Data/*.lua` not reviewed). Gate before: pass (b7081eb). Gate after: pass.
7 slices × applicable lenses = 66 envelopes in `.sift/runs/`, 72 findings REPORTED, 0 suppressed.
All envelopes validate INCOMPLETE at the final HEAD only because per-slice commits moved HEAD after
review (see Next); every one read 100% of its scope.

## Carried forward

None — first audit.

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven | Suppressed | Gate |
|---|---|---|---|---|---|---|---|
| `tools/` | 8/8 | 28 | 4 | 4 | 24 | 0 | pass |
| `tests/` | 1/1 | 3 | 2 | 1 | 1 | 0 | pass |
| `Model.lua Settings.lua Sources.lua` | 3/3 | 9 | 4 | 3 | 5 | 0 | pass |
| `Tooltip.lua RecipeList.lua Trainer.lua` | 3/3 | 7 | 1 | 0 | 6 | 0 | pass |
| `Prices.lua Core.lua` | 2/2 | 6 | 2 | 0 | 4 | 0 | pass |
| `Route.lua Shopping.lua` | 2/2 | 8 | 1 | 1 | 7 | 0 | pass |
| docs + config | 13/13 | 11 | 6 | 5 | 5 | 0 | pass |

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| `tests/` | unused `C_TradeSkillUI.GetAllRecipeIDs` stub (only reader `ns.LearnReagents`, never called by the harness) | dead-code | −3 |
| model | `RoundMoney` arm `copper >= 10000 and 100` identical to the next arm (exhaustive check −5..3,000,000) | dead-code | −1 |
| model | `RoundMoney` comment wrong at ≥100g; `RecipeSuggestions` comment said effective skill, code uses base | comment (stale) | ±0 |
| tools | `gen_sources.lua_string` → import `gen_recipes.lua_string` (superset escaping; identical on current data) | parallel | −4 |
| tools | `tools/README.md`: "three generators" → five; luacheck command; GetBaseProfessionInfo claim | stale-docs | ±0 |
| route | Trainable's comment moved from `ns.TrainingFor` to `Trainable`, repeated clause dropped | comment | −1 |
| docs | README dev block + CI sentence; refresh-data PR body lists all regenerated files | stale-docs | +2 |
| config | `.luacheckrc` `globals = { "arg" }` for tests/ unused | dead-code | ±0 |

Net: −4 lines across 9 files (20 added, 24 removed).

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `tests/model_spec.lua:407` | "incomplete live reagents use bundled fallback" cannot fail: recipe 10 is already cached, no event fires; mutating `Prices.lua:178` `recipe = nil` → `recipe = recipe` still passes 38001/38001 | fire `onEvent(frame, "TRADE_SKILL_LIST_UPDATE")` before the check |
| `Core.lua:84` | `pairs(ns.ProfessionSkillLines)` runs at file load; the table lives in `Data/Recipes.lua` (added be11638). After an update + `/reload` (toc not re-read) this errors before the "restart the game" guard at Core.lua:345 | move the loop inside the `ContinueOnAddOnLoaded` guard or guard it |
| `Prices.lua:343` | `IsOurSearch` returns true on empty results; if the player's own search returns nothing, `FinishSearch` stamps every pending item `{time=now}`, wiping scanned prices | require at least one result, or track the query sent |
| `Model.lua:271` | `RoundMoney(0)` returns 1 (`math.max(...,1)`); an all-free (gathered) recipe shows "1c" per skill-up | return 0 for 0 |
| `Settings.lua:113` | sort tooltip "Order of recipes within each category." — sorting flattens categories (RecipeList.lua:61) | user-facing text; reword |
| `Route.lua:471` | rank suffix re-implements `ns.RankText` and has drifted (two spaces vs one) | call `ns.RankText` (changes visible spacing) |
| `tools/gen_sources.py:96/481` | `classicdb()` called twice; with `--refresh` the dump downloads twice | pass the path through |
| `tools/gen_sources.py:26` | `VENDOR_ROW` matches integer prices only; gen_vendor writes floats (latent, no rows today) | accept decimals |
| `docs/curseforge.md:7,15,33` | store listing lacks route/shopping/trainer/reagent features; misdescribes Auctionator precedence; licence "GPL-3.0" vs "-or-later" | rewrite listing (user-facing) |

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `tools/gen_trainer.py:34,49`, `gen_sources.py:186,200,148` | `classicdb`/`teach_effects`/trainer-row parse/vendor join/loot expansion re-implement gen_thresholds/gen_trainer helpers, with drift | per-pair diff; regenerate all Data with `--offline` before/after and byte-compare |
| 5 generators | argparse `--refresh/--offline` preamble cloned (jscpd) | extract a `cli()` helper; byte-compare outputs |
| `Core.lua:225` | `ns.Describe` restates `ns.NetCost` formula | measure row-render cost of calling NetCost (recomputes cost/value) |
| Model/Route/Shopping/Tooltip/Prices | price source `gather/vendor/scan/auctionator` and bucket set bare strings in 5 files; "scan or auctionator = auction" rule twice | a `ns.PRICE_SOURCE` table; needs in-game check |
| `Core.lua:9`, `Model.lua:256` | `craftValue` has no options table, LoadDB never validates it; unknown → "vendor" silently | add options table like `SORT_OPTIONS` |
| `Trainer.lua:9` | `C_TooltipInfo and` guard | confirm in client that C_TooltipInfo always exists on Forever |
| `Tooltip.lua:107`, `Sources.lua:38,53` | `ns.FormatAge`/`NPCLocation`/`LocationText` used only in their own file | NearestNPC is used before definition, so `local` needs reordering |
| `Route.lua:1`, `Tooltip.lua:1`, `gen_sources.py:291` | oversized: Route mixes planning/list/tooltips/render/tab chrome | separate reviewed split (not an audit edit) |
| `README.md:21-22` | ~430- and ~200-word feature bullets | restructure into a section with a list |
| `tests/model_spec.lua:94` | per-sample checks duplicate the whole-table loop | keep or drop is a test-design call |

## Learning captured

- Rules added: none (no repeated mechanical shape; the price-source strings need judgment).
- Lenses added: none.
- Project skill updates: LuaLS `need-check-nil` at Prices.lua:345 and Route.lua:94/97 confirmed false positives; `ns.LearnReagents` live via `hooksecurefunc` in RecipeList.lua; user-visible strings (Settings tooltips, curseforge.md) are proposal-only.

## Next

Fix the `Core.lua:84` reload crash and the `Prices.lua:343` price wipe, each with a harness check (the model_spec harness can already load Prices.lua).
