# sift audit — SkillUp Forever @ 6494f4b

Scope: whole repo (generated `Data/*.lua` not reviewed). Gate before: pass. Gate after: pass.
Nine blind Claude reviewers (one per slice, plus standards) and an independent Codex pass over
`Route.lua`, `Shopping.lua`, `Prices.lua` and `Core.lua`. 66 findings REPORTED, 0 suppressed,
0 suppressions in the tree. Open PRs #26 (Sources/Route/Shopping/docs/route.md) and #29 (data)
were avoided where hunks would collide.

## Carried forward (2026-09-23-main.md)

| Item | Disposition |
|---|---|
| `Route.lua:144` rank fees picked by target | still present |
| `Route.lua:469` unpriced grey recipes block suggestions | still present |
| `Prices.lua:229` empty live schematic, no bundled data, costs 0 | still present |
| `tests/core_spec.lua:49` identical init stubs | still present; proven again (swap Core.lua:376-377, spec passes) |
| `tools/screenshots.py:116,144` port drift | still present |
| `tools/screenshots.py:160` Leatherworker/Miner vs Skinning | still present; `price()` also omits `Gathered()` (:177) |
| `Sources.lua:95` `vendorsOnly` always true | still present; left for #26, which rewrites Sources.lua |
| `Route.lua:144` literal `75` | still present |
| `docs/route.md:7` "trainers' fees win" covers recipe fees only | still present; route.md left for #26 |
| `Model.lua:233` owned-count callback always 0 in production | still present |
| `Prices.lua:49` / `Tooltip.lua:129` `DAY` twice | still present |
| Generator helper duplication; argparse preamble | still present; the preamble has drifted (gen_vendor has no `__main__` error handler) |
| `ns.Describe` restates `ns.NetCost` | still present, unproven (cost of recomputing) |
| Price-source strings; `craftValue`/`sortMode` unvalidated | still present; now Anti-patterns in the profile |
| `Trainer.lua:13` `C_TooltipInfo and` guard | still present, unproven (needs a client check) |
| Own-file `ns.*` helpers (FormatAge, NPCLocation, LocationText, NearestNPC, RouteTarget, ShowRecipe) | still present, unproven (definition order) |
| Oversized `Route.lua` | still present (both reviewers: split planning 19-151 into its own file) |
| Long README bullets | `README.md:21` is 67 words (WFA-10 ~60) |
| `tests/model_spec.lua:94` sample checks | still present; test-design call |

## Slices

| Slice | Files read | Reported | Proven | Changed | Unproven | Suppressed | Gate |
|---|---|---|---|---|---|---|---|
| `tools/gen_*`, `latest_build`, `changelog` | 7/7 | 14 | 2 | 2 | 12 | 0 | pass |
| `tools/` other + README | 6/6 | 8 | 4 | 1 | 4 | 0 | pass |
| `tests/` | 4/4 | 1 | 1 | 0 | 0 | 0 | pass |
| `Model Settings Sources List types/` | 7/7 | 7 | 3 | 2 | 4 | 0 | pass |
| `Tooltip RecipeList Trainer` | 3/3 | 7 | 0 | 0 | 7 | 0 | pass |
| `Prices Core` | 2/2 | 4 | 2 | 0 | 2 | 0 | pass |
| `Route Shopping` | 2/2 | 10 | 4 | 3 | 6 | 0 | pass |
| Codex: `Route Shopping Prices Core` | 4/4 | 3 | 2 | 0 | 1 | 0 | pass |
| docs + config | 20/20 | 9 | 8 | 7 | 1 | 0 | pass |
| standards (`wow-forever-addon`) | 13/13 | 3 | 2 | 0 | 1 | 0 | pass |

Envelopes validated FINDINGS before each commit; at the final HEAD the seven edited slices read
INCOMPLETE only because this audit changed their scope files.

## Changes

| Slice | Change | Category | Lines |
|---|---|---|---|
| tools | `SECONDARY_SKILLS` 129/185 already in `REQUIRED_SKILLS`; Thresholds.lua regenerates identical offline | dead-code | ±0 |
| tools | gen_sources/gen_trainer docstrings (= `--help`) name everything they write | stale-docs | +1 |
| tools | `tools/README.md` points at `BUILD` instead of a build number the refresh job rewrites | stale-docs | ±0 |
| model | `RoundMoney` comment: whole silver below 1g, not "two largest coins" | comment | ±0 |
| model | `list:Add` comment restated `SkillUpListEntry` and missed `wrap` | comment | −1 |
| route | `Craftable` one-caller forwarder inlined into `ns.NextCraft` | inline | −7 |
| route | `AddBands` comment described a line layout the tooltip no longer has | comment | ±0 |
| route | tracker attach check reuses `ns.TrackerAttached` | parallel | ±0 |
| config | nine dot-path `.pkgmeta` ignores: the pinned packager prunes dot paths first; v0.3.0 zip confirms | dead-code | −9 |
| docs | AGENTS.md and README name the CI `sift` job | stale-docs | +1 |

Net: −15 lines across 11 files (before the profile and this report).

Tried and reverted (the type gate needs them): replacing List.lua's `---@class SkillUpList` with
`---@type`, and deleting Shopping.lua `Attach`'s `if not module` guard.

## Bugs found, not fixed

| Location | Evidence | Suggested fix |
|---|---|---|
| `Route.lua:641` (Codex) | `NextCraft` batches the whole segment even when it crosses the cap before rank training: skill 70, cap 75, target 95 gives 25 crafts, 20 of them without skill-ups (LuaJIT repro with the real Model/Route) | bound the batch at the cap, or split segments at rank training |
| `Prices.lua:417` (Codex) | `FinishSearch` clears a stored price without invalidating `priceCache`; closing the AH mid-scan leaves `ns.Price` returning the cleared price (repro: 51 items, empty first batch, close during the second) | invalidate derived caches whenever `FinishSearch` writes |
| `Prices.lua:121` | a fresh "nobody listed it" scan (`{time=now}`) is dropped, so an older Auctionator price wins, against the comments at :115 and :409 | treat a copper-less fresh entry as newer than Auctionator's |
| `tools/gen_vendor.py:64` | `max(VendorStackCount, 1)` prices a stack of 0 as one unit, silently | raise on a zero stack count |
| `tools/gen_sources.py:111` | `zip(..., strict=False)` truncates a dump row whose column count differs | `strict=True` |
| `tools/gen_trainer.py:23` | `PROFESSION_SKILLS` hard-codes what `professions()` derives; a new profession gets no fees or ranks | derive from `professions()` |
| plus the eight still-present bugs above | | |

## Proposals (user-facing or published copy)

| Location | Proposal |
|---|---|
| `Core.lua:368` | WFA-6 says tell players to `/reload`, never restart; this says restart. Check in game whether `/reload` picks up new TOC files, then fix the message or the pack |
| `CHANGELOG.md:14-16` | WFA-12: lead each Unreleased "Fixed" bullet with a bold player-facing sentence |
| `SkillUpForever.toc:3` | `## Notes` describes 0.1.0; no costs, route, shopping list or trainer |
| `docs/route.md:7,11` | ~330- and ~180-word paragraphs starting lower-case; turn into bullets after #26 merges |
| `README.md:21` | trim the Levelling route bullet under 60 words |

## Unproven candidates

| Location | Candidate | What would settle it |
|---|---|---|
| `Tooltip.lua:287` | `Band()` re-derives `Model.Color` | a rewrite through `Model.Color` that is no longer than `Band` |
| `Tooltip.lua:38`, `:280`, `Route.lua:220` | band-name list three times | a shared `ns.BANDS`; check each copy's length (one omits grey on purpose) |
| `Shopping.lua:282,317` | recipe-name and item-name fallbacks copied from Route/Tooltip; three "item N" copies never request item data | one `ns.ItemName`; in-game check that names load |
| `Tooltip.lua:405` | `GameTooltip.RefreshData` guard | confirm in the Forever client |
| `Prices.lua:228,255,273` | data-table guards | Core.lua:367 does not check `ns.ItemSellPrices`; needs a full-load proof |
| `tools/lint_multivalue.py:13`, `gen_recipes.py:100` | bare token kinds / SpellEffect ids | named constants; judgment |
| `tools/README.md:95` | runtime-contract paragraph in the generators README | move to a doc beside the code it describes |

## Learning captured

- Rules added: none (no repeated shape a tool can match without judgment).
- Lenses added: none.
- Project skill: four specs described; ty baseline 6 stated once; hooks the live-root search missed
  (`OnOtherSearch`, `AddInitializedFrameCallback`); zones for `types/`, `List.lua` and the unlisted
  config files; packager prunes dot paths; LuaLS-only guards; data byte-compare and standards-pack
  commands; Codex second pass on tiers 5-6; an Anti-patterns list.

## Next

Fix the two Codex bugs (`Route.lua:641` cap batching, `Prices.lua:417` cache invalidation) with
`route_spec`/`prices_spec` checks, then the carried `Route.lua:144/469` pair after #26 merges.
