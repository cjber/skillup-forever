# sift audit (diff) — SkillUp Forever, v0.3.0..d7198d1, pre-0.4.0 release

Scope: the diff since v0.3.0, weighted to what the 2026-09-24 audit had not seen: #26 (Shortest Path
Forever waypoints, clickable tracker lines), #34 (settings through the secure delegate) and #38.
Generated `Data/*.lua` (#29) not reviewed. One reviewer, after `/simplify` and `/review`. Gate before
and after: pass.

## Changes

| Change | Category | Evidence |
|---|---|---|
| `Prices.lua` `FinishSearch` refreshes cached prices per batch | bug (carried, Codex) | new `prices_spec` check fails before the fix |
| `Route.lua` `ns.NextCraft` stops the batch at the skill cap | bug (carried, Codex) | new `route_spec` check fails before the fix (40 offered, 8 reach the cap) |
| `Sources.lua` `ns.NearestNPC` drops `vendorsOnly`: every caller passed `true` | dead-code (carried) | all three callers and the spec; type updated |

## Carried items

| Item | Disposition |
|---|---|
| `Route.lua:641` cap batching | fixed |
| `Prices.lua:417` cache after `FinishSearch` | fixed |
| `Sources.lua:95` `vendorsOnly` always true | fixed |
| `Route.lua:144` rank fees by target, literal `75`; `Route.lua:469` unpriced grey recipes | still present |
| `tests/core_spec.lua:49` identical init stubs | still present |
| `tools/screenshots.py:160,177` gathering and `Gathered()` port drift | still present |
| `tools/gen_vendor.py:64`, `gen_sources.py:111`, `gen_trainer.py:23` | still present (generator fixes change data; one PR each with a byte-compare) |
| `Model.lua:233` owned-count callback; `DAY` twice; `ns.Describe` vs `ns.NetCost` | still present |
| Proposals (Core.lua restart message, TOC `## Notes`, route.md paragraphs, README bullet) | still open; user-facing copy |
| Unproven candidates (Tooltip bands, item-name fallbacks, guards) | still unproven |

## Tried and reverted

`Settings.lua`'s change callback calls `ns.RefreshRecipeList()` after `ns.PricesChanged()`, which
already does; `settings_spec` asserts the call list, so the one-line saving isn't worth the spec churn.

## Next

`Route.lua:144` rank fees with a `route_spec` repro.
